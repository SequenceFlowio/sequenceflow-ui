import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Log in | SequenceFlow Commerce Support",
  description: "Log in op Support, de AI-klantenserviceworkspace van SequenceFlow Commerce.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
